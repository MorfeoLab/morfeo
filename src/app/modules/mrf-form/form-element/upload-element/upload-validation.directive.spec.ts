import { UploadValidationDirective } from './upload-validation.directive';

describe('UploadValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadValidationDirective();
    expect(directive).toBeTruthy();
  });
});
